export default NotificationType = {
    COMMON_NOTIFICATION: 1, //  (new branch, branch maintenance, ...);
    COMMERCIAL_NOTIFICATION: 2, //  (discount, promotion, new product, ...);
    ORDER_NOTIFICATION: 30
}